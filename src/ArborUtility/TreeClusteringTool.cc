  /// \file TreeClusteringTool.cc
/*
 *
 * TreeClusteringTool.cc source template automatically generated by a class generator
 * Creation date : mer. avr. 8 2015
 *
 * This file is part of ArborContent libraries.
 * 
 * ArborContent is free software: you can redistribute it and/or modify
 * it under the terms of the GNU General Public License as published by
 * the Free Software Foundation, either version 3 of the License, or
 * (at your option) any later version.
 * based upon these libraries are permitted. Any copy of these libraries
 * must include this copyright notice.
 * 
 * ArborContent is distributed in the hope that it will be useful,
 * but WITHOUT ANY WARRANTY; without even the implied warranty of
 * MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 * GNU General Public License for more details.
 * 
 * You should have received a copy of the GNU General Public License
 * along with ArborContent.  If not, see <http://www.gnu.org/licenses/>.
 * 
 * @author Remi Ete
 * @copyright CNRS , IPNL
 */

#include "Pandora/AlgorithmHeaders.h"

#include "ArborUtility/TreeClusteringTool.h"
#include "ArborHelpers/CaloHitHelper.h"
#include "ArborObjects/CaloHit.h"

namespace arbor_content
{

pandora::StatusCode TreeClusteringTool::BuildClusters(const pandora::Algorithm &parentAlgorithm) const
{
	const pandora::CaloHitList *pCaloHitList = NULL;
	PANDORA_RETURN_RESULT_IF(pandora::STATUS_CODE_SUCCESS, !=, PandoraContentApi::GetCurrentList(parentAlgorithm, pCaloHitList));

	PANDORA_RETURN_RESULT_IF(pandora::STATUS_CODE_SUCCESS, !=, this->BuildClusters(parentAlgorithm, pCaloHitList));

	return pandora::STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

pandora::StatusCode TreeClusteringTool::BuildClusters(const pandora::Algorithm &parentAlgorithm, const pandora::CaloHitList *const pCaloHitList) const
{
	if(NULL == pCaloHitList)
		return pandora::STATUS_CODE_INVALID_PARAMETER;

	if(pCaloHitList->empty())
		return pandora::STATUS_CODE_SUCCESS;

	for(pandora::CaloHitList::const_iterator iter = pCaloHitList->begin(), endIter = pCaloHitList->end() ;
			endIter != iter ; ++iter)
	{
		const CaloHit *const pCaloHit = dynamic_cast<const CaloHit *const>(*iter);

		if(NULL == pCaloHit)
			return pandora::STATUS_CODE_FAILURE;

		if(!pCaloHit->IsSeed() || !PandoraContentApi::IsAvailable(parentAlgorithm, pCaloHit))
			continue;

		PANDORA_RETURN_RESULT_IF(pandora::STATUS_CODE_SUCCESS, !=, this->BuildCluster(parentAlgorithm, pCaloHit));
	}

	return pandora::STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

pandora::StatusCode TreeClusteringTool::BuildCluster(const pandora::Algorithm &parentAlgorithm, const CaloHit *const pSeedCaloHit) const
{
	if(NULL == pSeedCaloHit)
		return pandora::STATUS_CODE_FAILURE;

	if(!pSeedCaloHit->IsSeed())
		return pandora::STATUS_CODE_INVALID_PARAMETER;

	pandora::CaloHitList clusterCaloHitList;
	clusterCaloHitList.insert(pSeedCaloHit);
	PANDORA_RETURN_RESULT_IF(pandora::STATUS_CODE_SUCCESS, !=, CaloHitHelper::BuildCaloHitList(pSeedCaloHit, FORWARD_DIRECTION, clusterCaloHitList));

	const pandora::Cluster *pCluster = NULL;
	PandoraContentApi::ClusterParameters clusterParameters;
	clusterParameters.m_caloHitList = clusterCaloHitList;

	PANDORA_RETURN_RESULT_IF(pandora::STATUS_CODE_SUCCESS, !=, PandoraContentApi::Cluster::Create(parentAlgorithm, clusterParameters, pCluster));

	return pandora::STATUS_CODE_SUCCESS;
}

//------------------------------------------------------------------------------------------------------------------------------------------

pandora::StatusCode TreeClusteringTool::ReadSettings(const pandora::TiXmlHandle xmlHandle)
{
	/* nop */
	return pandora::STATUS_CODE_SUCCESS;
}

} 

